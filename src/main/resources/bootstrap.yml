server:
  port: 8081

spring:
  application:
    name: productsearchserver
  profiles:
    active: local
---
spring:
  application:
    name: productsearchserver
  profiles:
    active: local
  cloud:
    config:
      enabled: false
---
spring:
  application:
    name: productsearchserver
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888

