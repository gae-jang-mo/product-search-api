server:
  port: 8081

spring:
  application:
    name: product-search-api
  profiles:
    active: local
---
spring:
  application:
    name: product-search-api
  profiles:
    active: local
  cloud:
    config:
      enabled: false
---
spring:
  application:
    name: product-search-api
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888

