server:
  port: 8081

spring:
  application:
    name: productsearchapi
  profiles:
    active: local
---
spring:
  application:
    name: productsearchapi
  profiles:
    active: local
  cloud:
    config:
      enabled: false
---
spring:
  application:
    name: productsearchapi
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888

