spring:
  application:
    name: product-search-api
  profiles:
    active: local
---
spring:
  profiles:
    active: local
  cloud:
    config:
      enabled: false
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888

